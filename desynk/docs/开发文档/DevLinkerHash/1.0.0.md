## 📄 DesynkHash 算法规范（v1.0）

### 1. 算法目标
- 对任意输入字符串生成**不可逆、唯一性高、抗彩虹表攻击**的哈希值。
- 盐值具有**时间绑定性**（每天变化一次），适用于需定期轮换盐值但又要求同天内结果一致的场景。
- 支持在 **TypeScript、Kotlin、C#** 中完全一致地实现。
- 人话：生成请求签名

---

### 2. 盐值构造规则

盐值（Salt）由两部分拼接而成：

salt = utc_day_timestamp + custom_salt_string

- `utc_day_timestamp`：**标准伦敦时区（UTC+0）当天的时间戳**（单位：秒），即 Unix 时间戳对 86400 取整。
  - 公式：`floor(current_unix_timestamp / 86400) * 86400`
  - 例如：2025-12-16T14:32:10Z → `1734336000`
- `custom_salt_string`：开发者传入的**固定自定义字符串**（建议为服务专属密钥，如 `"LingGongFang_Secret_2025"`）

> ✅ **注意**：盐值**不包含小时、分钟、秒**，确保同一天内多次调用结果一致。

---

### 3. 哈希流程

1. 获取当前 UTC 时间，计算 `utc_day_timestamp`  
2. 拼接盐值：`salt = utc_day_timestamp.toString() + custom_salt_string`  
3. 拼接原始输入与盐值：`input_with_salt = input_string + salt`  
4. 对 `input_with_salt` 使用 **UTF-8 编码**  
5. 计算 **SHA-256** 哈希值  
6. 返回 **十六进制小写字符串**（64 字符）

---

### 4. 跨平台实现代码

#### ✅ TypeScript
``` typescript
function getUTCDayTimestamp(): number {
  const now = new Date();
  const utcTimestamp = Math.floor(now.getTime() / 1000);
  return Math.floor(utcTimestamp / 86400) * 86400;
}

async function DesynkHash(
  input: string,
  customSalt: string
): Promise<string> {
  const utcDay = getUTCDayTimestamp();
  const salt = utcDay.toString() + customSalt;
  const data = input + salt;

  if (typeof window !== 'undefined' && window.crypto) {
    const encoder = new TextEncoder();
    const hashBuffer = await crypto.subtle.digest('SHA-256', encoder.encode(data));
    const hashArray = Array.from(new Uint8Array(hashBuffer));
    return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
  } else {
    const crypto = await import('crypto');
    return crypto.createHash('sha256').update(data, 'utf8').digest('hex');
  }
}
```


---

#### ✅ Kotlin
``` kt
import java.security.MessageDigest
import java.time.Instant
import java.time.ZoneOffset

object DesynkHash {
    fun hash(input: String, customSalt: String): String {
        val utcDayTimestamp = Instant.now().epochSecond / 86400 * 86400
        val salt = "$utcDayTimestamp$customSalt"
        val data = input + salt

        val digest = MessageDigest.getInstance("SHA-256")
        val hashBytes = digest.digest(data.toByteArray(Charsets.UTF_8))
        return hashBytes.joinToString("") { "%02x".format(it) }
    }
}
```


---

#### ✅ C#
``` csharp
public static class DesynkHash
{
    public static string Hash(string input, string customSalt)
    {
        var utcNow = DateTime.UtcNow;
        var utcDayTimestamp = ((long)utcNow.Subtract(DateTime.UnixEpoch).TotalSeconds / 86400) * 86400;
        var salt = utcDayTimestamp.ToString() + customSalt;
        var data = input + salt;

        using var sha256 = SHA256.Create();
        var hashBytes = sha256.ComputeHash(Encoding.UTF8.GetBytes(data));
        return BitConverter.ToString(hashBytes).Replace("-", "").ToLowerInvariant();
    }
}
```

---

### 5. 使用注意事项

| 项目 | 说明 |
|------|------|
| **时区** | 必须使用 **UTC+0（伦敦标准时间）**，不可用本地时区 |
| **跨平台一致性** | 三端实现经测试在相同输入+同一天下输出**完全一致** |
| **安全性** | 不可直接用于密码存储（因盐值可预测），适用于**防篡改校验、临时令牌生成、请求签名**等场景 |
| **性能** | SHA-256 快速，适合高频调用 |
| **自定义盐建议** | 使用高熵字符串（如 32 位随机字符），不要硬编码在客户端 |

---

### 6. 版本与维护

- **算法名称**：DesynkHash v1.0  
- **设计者**：Desynk 
- **适用场景**：API 请求签名、数据完整性校验、临时令牌生成  

---

